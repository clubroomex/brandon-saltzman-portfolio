{
  "name": "brandon-saltzman-portfolio",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "node build.js",
    "preview": "npm run build && npx --yes serve _site"
  },
  "dependencies": {
    "gray-matter": "^4.0.3",
    "js-yaml": "^4.1.0",
    "marked": "^12.0.2"
  }
}
